/**
 * Compatibility wrapper for the Star guard.
 * Shared Nitro auth now lives in /shared.
 */

export { requireAuth } from '../../shared/guards.js';
export { getProfileMeta } from '../../shared/profile.js';
