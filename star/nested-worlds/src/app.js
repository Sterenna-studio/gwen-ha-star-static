import{createPlanet,randomSeed,biosignal}from'./core/planet-generator.js';import{createSystem}from'./core/system-generator.js';import{System3DRenderer}from'./render/system-3d-renderer.js';import{dominantEvent}from'./core/orbital-events.js';import{analyse,discoveryId}from'./core/discovery-engine.js';import{countDiscoveries,saveDiscovery}from'./storage/codex-repository.js';import{mountCodex}from'./ui/codex-panel.js';
const $=s=>document.querySelector(s),seed=$('#seed'),temp=$('#temp'),readout=$('#readout'),planetView=$('#planet'),renderer=new System3DRenderer(planetView);let planet,system,codex,activeWorld=0;
function selected(){return system?.planets?.[activeWorld]||planet}function selectWorld(index){if(!system?.planets?.length)return;activeWorld=(index+system.planets.length)%system.planets.length;renderer.setActivePlanet(activeWorld);renderer.focusPlanet(activeWorld);update()}function selectionControls(){let nav=$('#world-navigation');if(nav)return;nav=document.createElement('div');nav.id='world-navigation';nav.setAttribute('aria-label','Navigation des mondes');nav.innerHTML='<button type="button" data-world="-1">← Monde précédent</button><button type="button" data-world="1">Monde suivant →</button>';readout.closest('aside').append(nav);nav.addEventListener('click',e=>{const step=Number(e.target.dataset.world);if(step)selectWorld(activeWorld+step)})}function update(){const t=+temp.value,report=analyse(planet,t),id=discoveryId(planet,report),discovery=report.species?.name||report.anomaly,event=dominantEvent(system,performance.now()),world=selected();if(id&&saveDiscovery({id,seed:planet.seed,title:discovery}))codex?.render();$('#temp-value').textContent=t;const status=$('#world-status');if(status)status.textContent=event.type==='CALME'?(discovery?'SIGNAL DÉTECTÉ':'SYSTÈME EN ORBITE'):event.type;const worldName=world?.name||world?.id||`MONDE ${activeWorld+1}`;const rows=[['SYSTÈME',planet.seed],['MONDE ACTIF',worldName],['TYPE',world?.type||planet.type],['BIOME',report.biome],['BIOSIGNAL',biosignal(planet,t)?'ACTIF':'SILENCIEUX'],['PHÉNOMÈNE',event.label],['DÉCOUVERTE',discovery||'AUCUNE'],['CODEX',countDiscoveries()+' ARCHIVE(S)']];readout.innerHTML=rows.map(([a,b])=>`<dt>${a} :</dt><dd>${b}</dd>`).join('');selectionControls()}function explore(value=seed.value){planet=createPlanet(value);system=createSystem(planet.seed);activeWorld=0;seed.value=planet.seed;renderer.setSystem(system);const u=new URL(location);u.searchParams.set('seed',planet.seed);history.replaceState({},'',u);update()}$('#form').addEventListener('submit',e=>{e.preventDefault();explore()});$('#random').onclick=()=>explore(randomSeed());temp.oninput=update;renderer.onSelect=selectWorld;seed.value=new URLSearchParams(location.search).get('seed')||seed.value;codex=mountCodex(readout.closest('aside'),update);explore();setInterval(update,800);