import{createRandom,hashString}from'./rng.js';
export function createLoopTree(seed){const random=createRandom(hashString(seed));const count=2+Math.floor(random()*4);return{seed,star:{energy:.45+random()*.55,hue:180+Math.floor(random()*100)},loops:Array.from({length:count},(_,index)=>{const moons=random()>.42?Math.floor(random()*3):0;return{id:`orbit-${index}`,radius:1+index*.95+random()*.45,speed:.18+random()*.8,phase:random()*Math.PI*2,weight:.2+random()*.8,fragments:random()>.7?18+Math.floor(random()*70):0,children:Array.from({length:moons},(_,moon)=>({id:`orbit-${index}-moon-${moon}`,radius:.16+moon*.12+random()*.12,speed:1+random()*2.4,phase:random()*Math.PI*2,weight:.05+random()*.14}))}})}}