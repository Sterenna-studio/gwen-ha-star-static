import{createRandom,hashString}from'./rng.js';
export function createLoopTree(seed){const random=createRandom(hashString(seed));const count=2+Math.floor(random()*4);const loops=Array.from({length:count},(_,index)=>{const frequency=.18+random()*.8,phase=random()*Math.PI*2,weight=.2+random()*.8;const moons=random()>.42?Math.floor(random()*3):0;return{id:`orbit-${index}`,radius:1+index*.95+random()*.45,frequency,phase,weight,amplitude:.04+random()*.22,tilt:(random()-.5)*.9,eccentricity:random()*.28,wobble:.15+random()*.7,subdivisions:1+Math.floor(random()*6),resonance:index?Math.max(1,Math.round(frequency/.18)):null,fragments:random()>.7?18+Math.floor(random()*70):0,children:Array.from({length:moons},(_,moon)=>({id:`orbit-${index}-moon-${moon}`,radius:.16+moon*.12+random()*.12,frequency:1+random()*2.4,phase:random()*Math.PI*2,weight:.05+random()*.14,amplitude:.02+random()*.08,tilt:(random()-.5)*1.4,eccentricity:random()*.18,wobble:.2+random()*.8,subdivisions:2+Math.floor(random()*5)}))}});return{seed,star:{energy:.45+random()*.55,hue:180+Math.floor(random()*100)},loops}}