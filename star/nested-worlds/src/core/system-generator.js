import{createLoopTree}from'./loop-tree-generator.js';
import{planetRadiusFromWeight,hasRingsFromWeight}from'./planet-size.js';
const TYPES=['MINÉRALE','OCÉANIQUE','VOLCANIQUE','CRYOGÉNIQUE','GAZEUSE'];
export function createSystem(seed){const tree=createLoopTree(seed);const starRadius=.35+tree.star.energy*.25;let cumulative=starRadius+.55;const planets=tree.loops.map((loop,index)=>{const radius=planetRadiusFromWeight(loop.weight),rings=hasRingsFromWeight(loop.weight),footprint=rings?radius*2.1:radius;cumulative+=footprint;loop.radius=cumulative;cumulative+=footprint+.5;return{id:`${seed}-p${index+1}`,type:TYPES[Math.floor(loop.weight*TYPES.length)%TYPES.length],radius,orbit:loop,moons:loop.children.map(child=>({id:child.id,radius:child.weight,orbit:child})),rings,asteroids:loop.fragments}});return{seed,star:{radius:starRadius,color:`hsl(${tree.star.hue} 90% 72%)`,energy:tree.star.energy},planets,belts:planets.filter(p=>p.asteroids).map(p=>({radius:p.orbit.radius,count:p.asteroids,spread:.12+p.orbit.weight*.18}))}}